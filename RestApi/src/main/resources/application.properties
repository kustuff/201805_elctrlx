# define DataSource properties 
# use h2 can have a buid in web console http://localhost:8080/h2-console

spring.h2.console.enabled=true
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:RestApiApplianceDB
spring.datasource.username=sa
spring.datasource.password=
# Does not work: not started without "classpath:" specified. And SQL are not executed if "classpath:" added.
  # spring.datasource.schema=classpath:schema.sql
  # spring.datasource.data=classpath:data.sql
spring.jpa.show-sql=true